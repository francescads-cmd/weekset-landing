export const FEATURES = {
  testimonials: false,
  pricing: false,
};

export const SUPPORTED_LANGS = ['it', 'en', 'es', 'de', 'fr'];
export const DEFAULT_LANG = 'it';

export const APP_URL = 'https://weekset.app';
